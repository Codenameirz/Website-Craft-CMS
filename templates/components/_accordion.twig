<div class="flex flex-col w-full h-screen" style="{{ blockContent.bgColor }}">
  <div class="grid md:grid-cols-2 w-11/12 gap-x-12 gap-y-4 m-auto">
    <div class="box-content w-11/12 text-center m-auto">
      
    </div>
    <div class="box-content w-11/12 text-center md:text-left m-auto">
      <h1 class="text-white text-2xl md:text-4xl font-bold">{{ blockContent.header }}</h1>
      <p class="text-white text-base md:text-lg my-3">{{ blockContent.body }}</p>
      <div x-data="{ openedIndex: false }" class="flex flex-col">
        <div @click="openedIndex == 1 ? openedIndex = -1 : openedIndex = 1" style="background-color: #212121;" class="flex items-center justify-between cursor-pointer border p-4">
          <p class="font-bold text-white">Apa itu infoGim?</p>
          <span :class="openedIndex == 1 ? 'la-angle-down' : 'la-angle-up'" class="las text-white"></span>
        </div>
          <div x-show="openedIndex == 1" class="border p-4 text-black bg-white bg-opacity-60">
          infoGim adalah website yang memuat informasi tentang game.
        </div>
        <div @click="openedIndex == 2 ? openedIndex = -1 : openedIndex = 2" style="background-color: #212121;" class="flex items-center justify-between cursor-pointer border p-4">
          <p class="font-bold text-white">Informasi apa saja yang ada di infoGim?</p>
          <span :class="openedIndex == 2 ? 'la-angle-down' : 'la-angle-up'" class="las text-white"></span>
        </div>
        <div x-show="openedIndex == 2" class="border p-4 text-black bg-white bg-opacity-60">
          infoGim berisi informasi tanggal rilis, developer, publisher, genre, mode, perspective, engine, dan platform bermain game.
        </div>
        <div @click="openedIndex == 3 ? openedIndex = -1 : openedIndex = 3" style="background-color: #212121;" class="flex items-center justify-between cursor-pointer border p-4">
          <p class="font-bold text-white">Apakah informasi yang ada di infoGim sesuai dengan fakta?</p>
          <span :class="openedIndex == 3 ? 'la-angle-down' : 'la-angle-up'" class="las text-white"></span>
        </div>
          <div x-show="openedIndex == 3" class="border p-4 text-black bg-white bg-opacity-60">
          Informasi yang ada di infoGim diambil dari beberapa sumber terpercaya, seperti wikipedia.
        </div>
      </div>
    </div>
  </div>
</div>