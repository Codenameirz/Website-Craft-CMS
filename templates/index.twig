{% extends 'layout/_base_layout.twig' %}

{% set games = craft.entries.section('games').limit(3) %}

{% block content %}

  <div class="flex flex-col w-full h-screen justify-center m-auto text-center text-white">
    <div class="grid md:grid-cols-3 justify-between m-auto"></div>
  </div>

{% endblock %}